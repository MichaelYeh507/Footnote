# Development/test dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1

# Token counting for corpus sizing and chunk budgets. Not a runtime dependency
# yet -- the pipeline sends whole documents. Moves to requirements.txt when the
# chunker lands.
tiktoken==0.13.0
regex==2026.7.19
