# Development/test dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1

# tiktoken and its regex dependency moved to requirements.txt on 2026-08-18:
# services/chunk_assembly.py counts chunk budgets with the model's own
# encoding, so they are runtime now.
